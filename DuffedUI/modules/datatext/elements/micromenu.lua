local D, C, L = select(2, ...):unpack()

local DataText = T["DataTexts"]
local format = format

local Update = function(self)

end

local Enable = function(self)	

end

local Disable = function(self)

end

--DataText:Register(L["Template"], Enable, Disable, Update)