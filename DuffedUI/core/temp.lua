-------------------------------------------------------
-- Temporary file to build DuffedUI from a clean UI! --
-------------------------------------------------------
local D, C, L = select(2, ...):unpack()